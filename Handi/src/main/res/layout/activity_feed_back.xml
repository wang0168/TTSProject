<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grayBackgroundColor"
    android:orientation="vertical"
    tools:context="tts.project.handi.activity.FeedBackActivity">

    <include layout="@layout/toolbar_item" />

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
           android:gravity="top"
            android:hint="您的建议对我们很重要..."
            android:padding="8dp"
            android:minLines="10"
            android:id="@+id/edit_feedback"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/shape_comment_tag" />

        <Button
            android:id="@+id/btn_submit"
            android:layout_width="match_parent"
            android:layout_height="@dimen/y80"
            android:layout_marginTop="@dimen/y60"
            android:background="@drawable/shape_order_button"
            android:text="@string/text_submit"
            android:textColor="@color/white"
            android:textSize="@dimen/y30" />
    </LinearLayout>
</LinearLayout>
